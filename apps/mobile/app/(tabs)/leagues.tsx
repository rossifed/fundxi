import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function LeaguesScreen() {
  return (
    <PlaceholderScreen
      title="Leagues"
      message="Private league standings and join codes — Phase 3 port."
    />
  );
}
