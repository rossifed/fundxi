import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function FixturesScreen() {
  return (
    <PlaceholderScreen
      title="Fixtures"
      message="Calendar, groups and knockout bracket — Phase 3 port."
    />
  );
}
