import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function ScreenerScreen() {
  return (
    <PlaceholderScreen
      title="Screener"
      message="Filterable player list with live valuations — Phase 3 port."
    />
  );
}
