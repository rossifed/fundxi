import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function PortfolioScreen() {
  return (
    <PlaceholderScreen
      title="Portfolio"
      message="Holdings, P&L and trade history — Phase 3 port."
    />
  );
}
