{
  "name": "fundxi-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "bun run --filter @fundxi/web dev",
    "build": "bun run --filter @fundxi/web build",
    "preview": "bun run --filter @fundxi/web preview",
    "tc": "bun run --filter '*' tc",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
