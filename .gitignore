node_modules
dist
.DS_Store
*.local
*.local.json
.vite
*.tsbuildinfo
SESSION.md
.claude/
.env
.env.*
.envrc
.envrc.*

# Expo runtime caches (device list, dev server settings, etc.)
.expo/
apps/*/.expo/

# Expo auto-generates a root tsconfig.json stub (extends expo/tsconfig.base)
# when the dev server runs from the repo root — not part of the source tree.
/tsconfig.json
